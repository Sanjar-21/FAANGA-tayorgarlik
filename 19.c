#include <stdio.h>
#include <string.h>
#include <stdbool.h>

int word_countr(char *strig, char *word)   ;

int main(void) {

    return 0;
}

int word_countr(char *strig, char *word)  {
    int slen = strlen(strig);
    int wlen = strlen(word);
    int end = slen - wlen + 1;

    int cout = 0;
    for(int i = 0; i < end; i++) {
        
    }
}